// Placeholder for Sprint 3's shared game services (score reporting, ranking,
// achievements, login/session, ad hooks). Sprint 1's metadata-mirroring
// registry (GameModule/registerGame/getGames) was removed here — Supabase is
// the single source of truth for game metadata, and platform-side game
// loading is handled by apps/web/lib/playable-games.ts +
// apps/web/components/game-player.tsx instead.
export {};
