import { FeedbackOpsDashboard } from "@/components/admin/feedback-ops-dashboard";

export const metadata = {
  title: "Feedback Intelligence — Re:Play Admin",
};

export default function AdminFeedbackPage() {
  return <FeedbackOpsDashboard />;
}
