import { FlagshipSnakeIoHub } from "@/components/flagship-snake-io-hub";

export const metadata = { title: "Replay Snake.io — Flagship" };

export default function FlagshipSnakeIoPage() {
  return (
    <main className="flex flex-1 flex-col">
      <FlagshipSnakeIoHub />
    </main>
  );
}
