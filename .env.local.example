# Supabase project credentials.
# The anon key is safe for client exposure, so it's prefixed with NEXT_PUBLIC_
# so Next.js inlines it for both server and client components.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
